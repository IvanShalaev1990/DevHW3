package org.calculator.sum;

public interface SumCalculator {
    /**
     * The method returns the sum of all prime numbers that are included in this number.
     *
     * @param num Given number.
     * @return int.
     */
    int sum(final int num);
}
